# Planner 适配阶段门禁验收报告

**评审日期：** 2026-08-26
**开发分支：** `codex/planner-exploration-ui-v1`  
**基线提交：** `63e9b602`
**实现状态：** 当前分支工作树
**隔离工作树：** `D:\Solver_demo_project_planner_exploration`  
**数据库版本：** `017_planner_runtime_state_targets`<br>
**契约版本：** `planner-shared-scenario/v1`

## 1. 总结

本轮在既有 Planner 适配基础上完成“当前状态 → 目标状态”运行输入改造，不新增表或字段。活动包恢复为纯分类/画布容器；正式目标由每次运行选择并固化在不可变有效快照中。

| 阶段 | 结果 | 关键证据 |
|---|---|---|
| P0 基线与契约 | 通过 | 指定提交独立工作树；Planner 第二轮 64/64 通过 |
| P1 数据底座与自动 ID | 通过 | 严格 API、UUID/展示号、主输出、事务及身份测试 |
| P2 包/状态包镜像 | 通过 | 两级层次、多包复用、异常结构测试；包不再承载目标 |
| P3 简化数据管理与活动-only 画布 | 通过 | 复用原 X6 画布；3 条 Playwright 用例；状态节点 DOM 数为 0；拖动/连线进入草稿 |
| P4 三引擎接入 | 通过 | 本次当前/目标状态生成有效快照；LEGACY/A*/GA 同哈希、独立运行、统一 Validator |
| P5 导入与迁移 | 通过 | 017 递归转换旧目标包、去重、审计标记和幂等测试；旧 Excel 继续兼容 |
| P6 回归与发布收口 | 通过 | 后端全量回归、生产构建、迁移 SQL 与排除项扫描 |

## 2. 自动化结果

| 项目 | 结果 | 说明 |
|---|---|---|
| Solver 后端全量回归 | 419/419 通过，28.46 秒 | SQLite 测试环境仅有既有外键 DROP 排序警告 |
| Planner 当前/目标状态定向回归 | 18/18 通过 | 场景服务、Excel、017 转换、API、三引擎有效快照和模块 X 90 分钟路径 |
| Planner 原项目 | 64/64 通过，152.401 秒 | 首轮有 1 项 3 秒 GA 预算波动；单项复跑及第二轮全量均通过 |
| 前端端到端 | 7/7 通过 | 当前/目标选择器、原 X6 活动-only 画布、包折叠整理、活动/事件绑定和原结果视图 |
| 前端生产构建 | 通过 | Vite 2534 模块；仅存在大包体积警告 |
| Alembic 017 运行目标迁移 | 通过 | PostgreSQL 从 016 升级成功；当前头与期望头均为 017，兼容检查问题数为 0 |
| 规模/循环投影 | 通过 | 100 活动、20 包、99/100 条边；投影小于 1 秒 |

### TESTA 真实浏览器验收

- X6 画布显示 3 个活动节点、4 个活动包容器和 2 条活动依赖，状态节点为 0；
- `LEGACY`、`ASTAR`、`GA` 均基于场景修订 r17 和同一快照求解；
- 三个引擎均返回 1 个 Validator `VALID` 方案，总工期 9、活动数 3；
- 求解页的甘特图、活动网络图、执行明细和指标标签均可打开；
- GA 在 2 秒预算下返回 `TIMEOUT_PARTIAL`，但包含 1 个合法方案，属于预算内正常部分结果。

## 3. G0 断言

- 数据页不再展示目标活动包、系统输出或初始激活控件；
- 求解页提供当前状态和目标状态多选，普通界面只显示业务名称；
- 活动改名保持身份，默认输出名同步；复制产生全新身份；
- 活动包与隐藏状态包同 UUID 后缀一一镜像，成员变化原子同步；
- 历史一级目标包在 017 中递归展开，重复活动只合并一个目标状态，旧字段清空；
- 画布只有活动节点，包只是容器，状态/状态包节点数固定为 0；
- 草稿任一操作失败时场景修订和对象均保持不变；
- 三引擎同一输入哈希，正式候选全部经过 Planner Validator；
- 本次选择、有效快照和哈希进入运行详情，不修改场景修订号；
- 迁移要求预览、确认和备份确认，旧表不写；
- 资源实例能力明确返回不支持，页面只显示容量汇总；
- 新菜单和生产构建不存在知识库版本、Manifest、历史回写或升级入口。

## 4. 已知观察项

- 复用 X6 与 ECharts 后前端主包未压缩约 2.77 MB，Vite 给出大于 500 kB 的非阻断警告；后续应按 Planner 数据页和求解页做动态拆包。
- SQLite 测试清表时对既有 `candidate_plan/solve_request` 循环外键产生排序警告，不影响 PostgreSQL 事务或测试结果。
- Planner 的一项 GA 质量测试使用 3 秒墙钟预算，首次在整套高负载下得到次优 117；单项复跑与第二轮 64 项全量均得到目标 111。建议 CI 为该套件保留稳定 CPU 配额。
- 资源实例、工作日历、数值效果、物料消耗和抢占不在本期，详见适配说明。

## 5. 签署

| 角色 | 结论 | 姓名/日期 |
|---|---|---|
| 产品 | 待签署 |  |
| 开发 | 待签署 |  |
| 测试 | 待签署 |  |
